import { type ReactNode, useEffect, useRef } from 'react';
import { AnimatePresence, motion } from 'framer-motion';

export function Reveal({children,delay=0}:{children:ReactNode;delay?:number}){return <motion.div initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.18}} transition={{duration:.55,delay}}>{children}</motion.div>}
export function SectionTitle({eyebrow,title,children}:{eyebrow:string;title:string;children?:ReactNode}){return <Reveal><p className="eyebrow">{eyebrow}</p><h2>{title}</h2>{children&&<div className="section-copy">{children}</div>}</Reveal>}
export function ConfirmModal({open,title,onCancel,onConfirm}:{open:boolean;title:string;onCancel:()=>void;onConfirm:()=>void}){const ref=useRef<HTMLButtonElement>(null);useEffect(()=>{if(open)ref.current?.focus()},[open]);return <AnimatePresence>{open&&<motion.div className="modal-backdrop" onMouseDown={onCancel} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.div className="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title" onMouseDown={e=>e.stopPropagation()} initial={{scale:.96,y:10}} animate={{scale:1,y:0}} exit={{scale:.96,y:10}}><p className="eyebrow">confirmar acción</p><h3 id="modal-title">{title}</h3><p>Esta acción no se puede deshacer.</p><div className="modal-actions"><button ref={ref} className="button ghost" onClick={onCancel}>cancelar</button><button className="button danger" onClick={onConfirm}>eliminar</button></div></motion.div></motion.div>}</AnimatePresence>}
